#include<iostream>
#include<string>
using namespace std;
string rev_string(string str){
    int start=0;
    string rev;
    int end=str.length()-1;
    for(int i=0;start<=end;i++){
        swap(str[start],str[end]);
    }
    if()
}